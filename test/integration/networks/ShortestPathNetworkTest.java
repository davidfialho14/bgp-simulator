package networks;

import core.Policy;
import policies.shortestpath.ShortestPathPolicy;

public class ShortestPathNetworkTest {

    protected Policy shortestPathPolicy = new ShortestPathPolicy();   // always the same shortestPathPolicy
}
